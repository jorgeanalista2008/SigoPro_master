{
  "name": "sigopro-master",
  "version": "1.0.0",
  "description": "Monorepo para SaaS de Contabilidad y Control Fiscal - Venezuela",
  "scripts": {
    "install-deps": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n \"backend,frontend\" -c \"blue,green\" \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
